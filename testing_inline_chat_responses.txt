Inline active-edit validation template:

Purpose:
- Verify inline chat applies edits directly to the active file (not only as text in the chat reply pane).
- Validate behavior with both @llama-local and @llama-vscode.

Environment checks:
- [ ] Ports healthy: 8009, 8011, 8012.
- [ ] Model endpoints configured in VS Code settings.
- [ ] File edit tools enabled:
- [ ] llama-vscode.tool_edit_file_enabled = true
- [ ] llama-vscode.tool_permit_file_changes = true
- [ ] Use inline chat from editor selection (Ctrl+I in active file).

Repro test case A (single-line edit):
- Active file content:
- `hello world from vscode`
- Inline prompt:
- `Replace this line with title case only. Apply edit to file directly.`
- Expected result in file:
- `Hello World From Vscode`
- Pass criteria:
- [ ] File buffer changed automatically.
- [ ] Diff is shown/applied in editor.
- [ ] No JSON/tool output-only response in chat pane.

Repro test case B (structured edit):
- Active file content:
- `name: vscode inline test`
- Inline prompt:
- `Convert to YAML with keys name and status where status is ready. Edit file directly.`
- Expected result in file:
- `name: vscode inline test`
- `status: ready`
- Pass criteria:
- [ ] Edit applied to active file.
- [ ] No instruction-only response without file mutation.

Repro test case C (selection-bound edit):
- Select only one paragraph in the file.
- Inline prompt:
- `Rewrite selected text for clarity, keep meaning.`
- Pass criteria:
- [ ] Only selected region is changed.
- [ ] Unselected text remains unchanged.

If response appears only in chat pane:
- Re-run settings bootstrap:
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Configure-LlamaVscode.ps1`
- Re-check endpoints:
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Validate-InlineChatFeatures.ps1`
- Ensure inline invocation is from active editor selection, not side chat panel.

Session notes:
- Agent used:
- Model used:
- Result:
- Follow-up fix needed:
